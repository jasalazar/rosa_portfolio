<%= form_for(@admin_artist) do |f| %>
  <% if @admin_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_artist.errors.count, "error") %> prohibited this admin_artist from being saved:</h2>

      <ul>
      <% @admin_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sinopsis %><br>
    <%= f.text_area :sinopsis %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
